<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
	<channel>
		<title>Posts on Yash Lala</title>
		<link>https://yashlala.github.io/posts/</link>
		<description>Recent content in Posts on Yash Lala</description>
		<generator>Hugo -- gohugo.io</generator>
		<language>en-us</language>
		<lastBuildDate>Tue, 05 Jan 2021 14:24:25 -0800</lastBuildDate>
		<atom:link href="https://yashlala.github.io/posts/index.xml" rel="self" type="application/rss+xml" />
		
		<item>
			<title>Building Knuth-Morris-Pratt (KMP) Pattern Matching</title>
			<link>https://yashlala.github.io/posts/kmp-pattern-matching/</link>
			<pubDate>Tue, 05 Jan 2021 14:24:25 -0800</pubDate>
			
			<guid>https://yashlala.github.io/posts/kmp-pattern-matching/</guid>
			<description>= Context
The KMP algorithm describes a method for finding a length-m string in a length-n string in O(m+n) time.
There are plenty of online resources describing the algorithm&amp;rsquo;s implementation. I couldn&amp;rsquo;t find many satisfactory explanations as to why it works, so I decided to write up some notes myself.
This post documents an effort to construct the KMP Algorithm from the ground up. It&amp;rsquo;ll focus on the intuitions behind the process; to get familiar with the mechanics of the algorithm, I&amp;rsquo;d recommend watching Tushar Roy&amp;rsquo;s excellent two-part video on the topic https://youtu.</description>
			<content type="html"><![CDATA[<p>= Context</p>
<p>The KMP algorithm describes a method for finding a length-<strong>m</strong> string in
a length-<strong>n</strong> string in <em>O(m+n)</em> time.</p>
<p>There are plenty of online resources describing the algorithm&rsquo;s
<em>implementation</em>. I couldn&rsquo;t find many satisfactory explanations as to why it
works, so I decided to write up some notes myself.</p>
<p>This post documents an effort to construct the KMP Algorithm from the ground
up. It&rsquo;ll focus on the intuitions behind the process; to get familiar with the
mechanics of the algorithm, I&rsquo;d recommend watching Tushar Roy&rsquo;s excellent
two-part video on the topic <a href="https://youtu.be/GTJr8OvyEVQ%5Bhere%5D">https://youtu.be/GTJr8OvyEVQ[here]</a> and
<a href="https://youtu.be/KG44VoDtsAA%5Bhere%5D">https://youtu.be/KG44VoDtsAA[here]</a>.</p>
<p>= Designing KMP</p>
<p>The KMP algorithm operates in linear time.</p>
<p>To stay in our runtime bounds, assume we only make one pass over the string to
search. By looking through</p>
<p>This will make our algorithms incorrect at first; just bear with me for now.</p>
<p>Given a string <code>a</code> and a string <code>b</code>, find the first occurrence of <code>b</code> in <code>a</code>.</p>
<p>NOTE: We&rsquo;ll be referring to <code>a</code> as the <em>string</em> and <code>b</code> as the <em>pattern</em> from
now.</p>
<p>== KMP v0.1</p>
<p>Our first version of the algorithm will function as follows:</p>
<ol>
<li>Given a string <code>a</code> and a string <code>b</code>,</li>
</ol>
<p>Let&rsquo;s see how our algorithm performs on some examples.</p>
<p>&hellip;.
Given <code>abcyash</code>, find <code>yash</code>.
&hellip;.</p>
<p>Pretty simple; go through, find a <code>y</code>, keep matching, and finish.
Let&rsquo;s move on.</p>
<blockquote>
<p>Given <code>yeemyeelyeefyeet</code>, find <code>yeet</code>.</p>
</blockquote>
<p>Again, pretty simple; go through, find a <code>y</code>, keep matching.
If our match fails, just keep on going through the string looking for more
<code>y</code>s as before.</p>
<p>This case illustrates our first issue any &ldquo;one-pass&rdquo; algorithm must encounter:</p>
<ol>
<li><em>Sometimes, we&rsquo;ll get a mismatch only at the end of a pattern</em>.</li>
</ol>
<p>This isn&rsquo;t always a problem.
In our example, we scan for the letter <code>y</code>. If we find <code>y</code>, then we have found
a &ldquo;candidate string&rdquo; for <code>yeet</code> (ie. the <code>y</code> could potentially be the start of
the word <code>yeet</code>). To determine if our candidate string is valid, we continue
matching until we either succeed finding <code>yeet</code> or find a mismatch (eg.
<code>yeel</code>).</p>
<p>Take a look at our numbered candidate strings below:</p>
<pre><code>yeem yeel yeef yeet
1--- 2--- 3--- 4---
</code></pre><p>However, there are some &ldquo;problem strings&rdquo; for which this approach doesn&rsquo;t
work. Consider the following example:</p>
<blockquote>
<p>Given <code>eteted</code>, find <code>eted</code>.</p>
</blockquote>
<p>Our algorithm will fail on this input.
First, we match the <code>ete</code> with <code>ete</code>, then find that <code>t != d</code>.
But we can&rsquo;t keep going just yet; we&rsquo;ll miss the pattern!
Rather than start scanning for a <code>e</code> again, we have to step <em>back</em>
and look at the <code>e</code> we&rsquo;ve most recently scanned.
Let&rsquo;s take a look at our numbered candidate strings to illustrate:</p>
<pre><code>eteted
1---
  2---
</code></pre><p>This brings us to Issue 2:</p>
<ol start="2">
<li><em>Sometimes, candidate strings can overlap</em>.</li>
</ol>
<p>What causes a candidate string to overlap? We see that it depends entirely on
the string that we&rsquo;re searching for; possible matches for <code>yash</code> will never
overlap with each other. Let&rsquo;s try to isolate exactly what properties of the
search pattern cause this to happen.</p>
<p>For two candidate strings to overlap as above, the pattern must overlap with
itself (at least partially). <code>yash</code>, for example, can never overlap with
itself (it starts with a <code>y</code>, which is only present once in the string). To
state more formally, two candiate strings can &ldquo;overlap&rdquo; if some part of the
pattern is also a prefix to the pattern.</p>
]]></content>
		</item>
		
	</channel>
</rss>
